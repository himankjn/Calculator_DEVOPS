---
- name: Pull docker image of Calculator
  hosts: all
  tasks:
    - name: Pull image
      docker_image:
        name: himankjn/check
        source: pull
    - name: Create a data container
      docker_container:
        name: mydata
        image: busybox
